/*
 * RegisterForm Messages
 *
 * This contains all the text for the RegisterForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RegisterForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RegisterForm container!',
  },
});
